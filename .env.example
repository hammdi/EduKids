# Example .env for EduKids
# Copy this file to .env (at the project root) and replace values with real secrets.

# Mistral API key (required for the assistant to work)
MISTRAL_API_KEY=2WC2TOx7fBperEqMgasE390GYC0Isenq

# Optional: override Mistral endpoint
# MISTRAL_API_URL=https://api.mistral.ai/v1/chat/completions

# Django secret key (development only — keep secret in production)
# SECRET_KEY=django-insecure-...
